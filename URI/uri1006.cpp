#include <cstdio>
#include <iostream>

using namespace std;

int main()
{
    float a, b, c;
    cin >> a >> b >> c;
    printf("MEDIA = %.1f\n", (a * 2.0f + b * 3.0f + c * 5.0f) / 10.0f);

    return 0;
}
